// Exemplo para iniciar a música automaticamente ao carregar a página (se permitido pelo navegador)
window.onload = function () {
    const audio = new Audio('src/music/amor.mp3');
    audio.play();
};

